$(function() {
  $("#announcements").sortable({
    axis: "y",
    update: function() {
      $("#saving").fadeIn();
      $.post($(this).data("update-url"), $(this).sortable("serialize")).done(function() {
        $("#saving").fadeOut();
      })
    }
  });
});